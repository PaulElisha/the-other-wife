/** @format */

// This file exposes the Express app to Vercel's serverless runtime.
// Vercel's @vercel/node builder will compile TypeScript in the api/ directory
// so you can keep .ts imports in your source files.

import app from "../app";

export default app;
